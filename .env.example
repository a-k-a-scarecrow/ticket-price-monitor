# Reference only — for local runs, export these in your shell (see README).
# In GitHub Actions they come from repo Secrets instead, never from this file.

TICKETMASTER_API_KEY=
SEATGEEK_CLIENT_ID=
DISCORD_WEBHOOK_URL=
